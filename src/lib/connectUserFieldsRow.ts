/** Row shape for calendar/lab grids after tutors-connect-users enrichment. */
export type ConnectUserFieldsRow = {
  studentid?: string;
  full_name?: string;
  avatar_url?: string;
  online_status?: string;
  sentiment?: string;
};
